# Creating nested tuples
inner_tuple1 = (1, 2, 3)
inner_tuple2 = ('a', 'b', 'c')
outer_tuple = (inner_tuple1, inner_tuple2)

# Display the nested tuple
print("Nested Tuple:")
print(outer_tuple)
